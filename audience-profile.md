# Audience Profile — 写作品味锚

> **用途**:`/research`(综合 outline 时)和 `/draft`(写稿时)都以这份文件为品味锚。
> **来源**:这是 content-ops 仓库根 `audience-profile.md` 的 vault 快照。**content-ops 版为权威**,定期同步;本地改动若与 content-ops 冲突,以 content-ops 为准。
> **同步方式**:content-ops 更新后,把最新快照覆盖到这里(handoff §5)。

---

## 1. 受众画像(写给谁)

**核心读者 = 全背景 AI builder**,锚点是「**能上手但非资深工程师**」:

- 会用 Claude Code / Cursor / API,能跑通脚本,但不是 systems engineer。
- 关心「怎么把这个能力用进我的工作流」远多于「这个东西底层怎么实现」。
- 时间稀缺,先要 takeaway,再决定是否深读细节。
- 跨语言:中英混读无障碍,但中文是母语阅读速度。

**分层深度 + 角色化入口**(同一篇文章服务多种读者):

- **分层深度**:开头给「一句话价值 + 谁该读」;主体给可执行做法;深水区(实现细节、边界、反例)放后段或可折叠,让浅读者能停、深读者能挖。
- **角色化入口**:在合适处标明「如果你是 X(独立 builder / 内容创作者 / 团队 lead),这一段对你意味着……」,让不同角色快速定位与自己相关的部分。

## 2. Voice(怎么写)

取自 owner(vfan,新加坡 builder,growth marketer + 独立 AI 内容创作者):

- **Bilingual EN/ZH**:中文为主体叙述;英文只在两处出现——(a) 概念原名(中文译名后括注),(b) 关键原文引语(保留英文 + 附中文意译)。**不要把整段中文再翻译成英文重复**。
- **Concise, action-oriented**:能用动词就不用名词堆砌;每个观点尽量带「所以你可以做什么」。
- **具体压过笼统**:保留具体数字、具体工具名、具体步骤(「102 features in 7 days」比「many features」强 10 倍)。
- **诚实**:有边界条件、失败模式、反对意见就写出来,不粉饰。

## 3. GEO / AEO 写作规则(为「被 AI 引用」而写)

目标是让文章既好读、又容易被 AI 搜索/回答引用(Generative Engine Optimization)。综合 outline 和写稿时**主动套用**以下规则,并在 outline 里**显式标注**该在哪里加:

- **加引用来源**(链接到官方文档/原始出处):约 **+40% AI 引用率**。→ 关键 claim 后面带出处。
- **加统计数据 / 具体数字**:约 **+37% AI 引用率**。→ 用可核验的量化数据支撑论点。
- **结构化、自包含的段落**:每段能独立回答一个子问题(便于被抽取为答案片段)。用清晰的小标题 / 问题式标题。
- **明确的定义句**:核心概念给一句「X 是……」的干净定义(circular definition 不算)。
- **前置结论**:段落/章节先给结论再展开论证(倒金字塔),方便摘要式引用。
- **可扫描**:善用列表、对比表、callout;避免长段不分行。

> **写稿落地提醒**(给 `/draft`):outline 里标了「此处加引用 / 此处加统计数据」的位置,正文**必须真的填上**对应的来源链接和数字——否则 GEO 规则等于没生效。
